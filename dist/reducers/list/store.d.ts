import { Config } from "../../types";
declare var _default: {
    assert: (scope: string, current: any[]) => void;
    remove: (config: Config, current: any[], addedRecord: any) => any[];
    merge: (current: any, records: any, key: any) => any;
};
export default _default;
