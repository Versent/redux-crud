import { ReducerName } from './types'

const CREATE_ERROR: ReducerName = "createError"

export default {
	DEFAULT_KEY:       "id",
	STORE_LIST:        "STORE_LIST",
	STORE_MAP:         "STORE_MAP",
	REDUCER_NAMES: {
		CREATE_ERROR,
	}
}
